/**
 * Admin actions
 *
 * This is a barrel file that re-exports all admin-related actions
 * from their domain-specific modules.
 */

// Re-export everything from the admin folder
export * from "./admin/index"
